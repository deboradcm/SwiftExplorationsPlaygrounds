//hasPrefix("Era")
/*:
 Também não é possível usar um método de instância em uma instância do tipo errado. Só é possível usar métodos que façam parte de um tipo específico ou sejam _membros_ dele.

 - callout(Experimente): Apague as barras de comentário da linha de código abaixo para ver o erro. Quando terminar, adicione as barras de comentário novamente.\
Desta vez, o erro é “Value of type 'Int' has no member 'hasPrefix'” (O valor do tipo 'Int' não tem membro chamado 'hasPrefix'). Isso significa que não há um método de instância `hasPrefix` no tipo `Int`.
 */
//let number = 42
//number.hasPrefix("Era")
/*:
 A seguir, veja os valores que os tipos podem conter.

[Anterior](@previous)  |  página 5 de 17  |  [Na sequência: Propriedades](@next)
 */
